---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - public/img/services/elektrikli-el-aletleri.webp
  - public/img/services/is-guvenligi.webp
  - public/img/services/nalburiye.webp
  - public/img/services/tesisat-malzemeleri.webp
  - public/img/services/yapi-kimyasallari.webp
  - public/img/services/yapi-malzemeleri.webp
  - src/pages/ServicesPage.jsx
  - scripts/convert-images.mjs
  - package.json
autonomous: true
requirements: [FOUN-02]
user_setup: []

must_haves:
  truths:
    - "All 6 service card image paths resolve to WebP files — no PNG 404s, no fallback to uncompressed sources"
    - "ServicesPage references the 6 images as /img/services/*.webp public path strings, not ES module PNG imports"
    - "Each WebP is meaningfully smaller than its 1.2-2.2 MB PNG source (target < 100 KB)"
  artifacts:
    - path: "public/img/services/elektrikli-el-aletleri.webp"
      provides: "WebP for Elektrikli El Aletleri card"
    - path: "public/img/services/is-guvenligi.webp"
      provides: "WebP for Is Guvenligi card"
    - path: "public/img/services/nalburiye.webp"
      provides: "WebP for Nalburiye card"
    - path: "public/img/services/tesisat-malzemeleri.webp"
      provides: "WebP for Tesisat Malzemeleri card"
    - path: "public/img/services/yapi-kimyasallari.webp"
      provides: "WebP for Yapi Kimyasallari card"
    - path: "public/img/services/yapi-malzemeleri.webp"
      provides: "WebP for Yapi Malzemeleri card"
    - path: "src/pages/ServicesPage.jsx"
      provides: "6 const path-string declarations replacing 6 ES module PNG imports; serviceImg1-6 names preserved"
      contains: "/img/services/elektrikli-el-aletleri.webp"
  key_links:
    - from: "src/pages/ServicesPage.jsx serviceImg1-6"
      to: "public/img/services/*.webp"
      via: "const serviceImgN = \"/img/services/....webp\""
      pattern: "serviceImg1 = \"/img/services/elektrikli-el-aletleri.webp\""
    - from: "ServicesPage services array imgSrc"
      to: "serviceImg1-6 constants"
      via: "existing imgSrc references (unchanged)"
      pattern: "imgSrc:\\s*serviceImg[1-6]"
---

<objective>
Build the WebP images vertical slice of the Foundation walking skeleton. After this plan, all 6 service card images resolve to compressed WebP files served as static public assets, and ServicesPage references them by public path string instead of ES module PNG imports. This proves the image-optimization half of the asset pipeline and removes ~12 MB of uncompressed PNGs from the bundle path.

Purpose: Removes the largest image payload (six 1.2-2.2 MB PNGs) and establishes the public-path WebP convention every later image addition will follow.
Output: 6 WebP files in `public/img/services/`, updated `src/pages/ServicesPage.jsx` (imports → const path strings), one-off conversion script, cleaned `package.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-RESEARCH.md
@CLAUDE.md
@src/pages/ServicesPage.jsx

<interfaces>
<!-- The 6 variable names serviceImg1..serviceImg6 are a contract: the existing `services` -->
<!-- array (ServicesPage.jsx lines 18-55) consumes them as imgSrc values and must NOT change. -->

camelCase PNG source → kebab-case WebP target → const path string (UI-SPEC WebP Image Convention):
  serviceImg1: src/img/services/elektrikliElAletleri.png → /img/services/elektrikli-el-aletleri.webp
  serviceImg2: src/img/services/isGuvenligi.png         → /img/services/is-guvenligi.webp
  serviceImg3: src/img/services/nalburiye.png           → /img/services/nalburiye.webp
  serviceImg4: src/img/services/tesisatMalzemeleri.png  → /img/services/tesisat-malzemeleri.webp
  serviceImg5: src/img/services/yapiKimyasallari.png    → /img/services/yapi-kimyasallari.webp
  serviceImg6: src/img/services/yapiMalzemeleri.png     → /img/services/yapi-malzemeleri.webp

Verbatim convert-images.mjs script: 01-PATTERNS.md lines 285-315.
Import-swap before/after: 01-PATTERNS.md lines 180-232.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert 6 service PNGs to WebP via sharp</name>
  <files>scripts/convert-images.mjs, public/img/services/elektrikli-el-aletleri.webp, public/img/services/is-guvenligi.webp, public/img/services/nalburiye.webp, public/img/services/tesisat-malzemeleri.webp, public/img/services/yapi-kimyasallari.webp, public/img/services/yapi-malzemeleri.webp, package.json</files>
  <read_first>
    - .planning/phases/01-foundation/01-PATTERNS.md (scripts/convert-images.mjs section, lines 285-315 — verbatim script and source→dest mapping)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 4 lines 273-298; Package Legitimacy Audit lines 72-83; sharp install note lines 494)
    - .planning/phases/01-foundation/01-UI-SPEC.md (WebP Image Convention, lines 249-281 — filename mapping, quality 82 / effort 6, < 100 KB target)
    - package.json (confirm "type": "module" and current devDependencies)
  </read_first>
  <action>
    Install temporary dev dependency: run `npm install --save-dev sharp`. Sharp's `install` script builds native bindings — this is expected, documented behavior (RESEARCH security note), not a postinstall network hook.
    Create `scripts/convert-images.mjs` using the verbatim ESM script in 01-PATTERNS.md lines 292-310: it `mkdirSync('public/img/services', { recursive: true })`, then for each of the 6 source→dest pairs runs `sharp(src).webp({ quality: 82, effort: 6 }).toFile(dest)`. Use the exact source paths (camelCase PNGs in `src/img/services/`) and exact dest paths (kebab-case WebP in `public/img/services/`) from the `<interfaces>` mapping.
    Run it with `node scripts/convert-images.mjs`.
    Verify exactly 6 WebP files were produced: `ls public/img/services/*.webp | wc -l` returns 6. Each WebP should be < 100 KB (sources are 1.2-2.2 MB; a result over 200 KB signals a quality/effort misconfiguration — investigate before proceeding).
    Do NOT delete or modify the source PNGs in `src/img/services/` — they stay in place (only ServicesPage.jsx changes, per RESEARCH Pattern 5).
    After WebP files are committed and verified, remove `sharp` from devDependencies: run `npm uninstall sharp`. Keep the committed script as a reproducible utility.
  </action>
  <verify>
    <automated>test $(ls public/img/services/*.webp | wc -l) -eq 6 && ls scripts/convert-images.mjs && for f in public/img/services/*.webp; do test $(stat -f%z "$f") -lt 204800 || { echo "TOO BIG: $f"; exit 1; }; done</automated>
  </verify>
  <acceptance_criteria>
    - `ls public/img/services/*.webp | wc -l` returns exactly 6
    - `ls public/img/services/elektrikli-el-aletleri.webp` exits 0
    - `ls public/img/services/is-guvenligi.webp` exits 0
    - `ls public/img/services/nalburiye.webp` exits 0
    - `ls public/img/services/tesisat-malzemeleri.webp` exits 0
    - `ls public/img/services/yapi-kimyasallari.webp` exits 0
    - `ls public/img/services/yapi-malzemeleri.webp` exits 0
    - Each WebP file is < 204800 bytes (200 KB ceiling; ~ <100 KB expected)
    - `ls src/img/services/elektrikliElAletleri.png` exits 0 (source PNGs preserved)
    - `grep -q '"sharp"' package.json` exits 1 (sharp removed from package.json)
    - `ls scripts/convert-images.mjs` exits 0
  </acceptance_criteria>
  <done>6 kebab-case WebP files exist in public/img/services/, each < 200 KB; source PNGs untouched; sharp removed from devDependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Swap ServicesPage PNG imports for WebP public path strings</name>
  <files>src/pages/ServicesPage.jsx</files>
  <read_first>
    - src/pages/ServicesPage.jsx (lines 1-55 — current 6 ES imports at lines 5-10 and the `services` array that consumes serviceImg1-6)
    - .planning/phases/01-foundation/01-PATTERNS.md (ServicesPage.jsx section lines 180-232 — before/after, variable mapping, import-ordering note)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 5 lines 300-319; Pitfall 3 lines 384-388 — preserve variable names)
  </read_first>
  <action>
    Replace the 6 ES module PNG import statements (current lines 5-10) with 6 `const` path-string declarations using the exact `serviceImg1`-`serviceImg6` variable names mapped to the kebab-case WebP public paths from the `<interfaces>` block: e.g. `const serviceImg1 = "/img/services/elektrikli-el-aletleri.webp";` through `const serviceImg6 = "/img/services/yapi-malzemeleri.webp";`.
    Preserve the variable names EXACTLY — the `services` array (lines ~18-55) uses them as `imgSrc` values and must NOT be modified (Pitfall 3: deleting imports without equivalent const bindings breaks the build with "serviceImg1 is not defined").
    Honor ES module ordering: `import` statements must not appear after a `const`. Place the 6 `const` declarations AFTER the last `import` line (after the `import Button from "../components/Button";` line at ~line 14, before `const canonical`). Do not leave any `import ... from "../img/services/*.png"` statements behind — all 6 PNG imports are removed.
    Change nothing else in the file (SEO props, motion variants, JSX, the services array data are all out of scope for Phase 1).
  </action>
  <verify>
    <automated>npm run build && test $(grep -c "img/services/.*\.png" src/pages/ServicesPage.jsx) -eq 0 && grep -q 'serviceImg1 = "/img/services/elektrikli-el-aletleri.webp"' src/pages/ServicesPage.jsx && test $(grep -c "/img/services/.*\.webp" src/pages/ServicesPage.jsx) -eq 6</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 (no import/reference errors)
    - `grep -c "img/services/.*\.png" src/pages/ServicesPage.jsx` returns 0 (no PNG references remain)
    - `grep -c "/img/services/.*\.webp" src/pages/ServicesPage.jsx` returns 6
    - `grep -q 'serviceImg1 = "/img/services/elektrikli-el-aletleri.webp"' src/pages/ServicesPage.jsx` exits 0
    - `grep -q 'serviceImg6 = "/img/services/yapi-malzemeleri.webp"' src/pages/ServicesPage.jsx` exits 0
    - `grep -c "imgSrc: serviceImg" src/pages/ServicesPage.jsx` returns 6 (services array unchanged, still references the 6 vars)
    - `grep -c "^import .*img/services" src/pages/ServicesPage.jsx` returns 0 (PNG import lines removed)
  </acceptance_criteria>
  <done>ServicesPage references all 6 images as /img/services/*.webp public path strings via const serviceImg1-6; no PNG imports remain; the services array still consumes the same 6 variables; npm run build succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local build → npm registry | `sharp` is installed and runs native image conversion |
| src/ PNG sources → public/ WebP outputs | conversion writes new binary files into the served public/ tree |
| public/ directory → production dist/ | WebP files are copied verbatim into the deployed bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Tampering | `sharp` npm install + conversion script | mitigate | Package vetted in RESEARCH Package Legitimacy Audit (67.5M wk downloads, official repo lovell/sharp, 12-yr history). `install` script builds native libvips bindings — expected/documented, no out-of-project network calls beyond fetching its own prebuilt binary. Installed as temporary devDependency and uninstalled after conversion. Output count gated to exactly 6 and size-gated (<200 KB) to detect malformed output. |
| T-01-05 | Denial of Service | Oversized WebP output inflating bundle | mitigate | Per-file size assertion (<200 KB; ~<100 KB expected vs 1.2-2.2 MB PNG sources) catches a quality/effort misconfiguration that would defeat the optimization goal. |
| T-01-06 | Information Disclosure | WebP metadata leakage | accept | sharp strips metadata by default on WebP output; images are public marketing assets with no sensitive EXIF/GPS expectation. Low risk. |
| T-01-SC | Tampering | npm install (sharp) | mitigate | RESEARCH Package Legitimacy Audit present and complete. sharp disposition Approved (not [ASSUMED]/[SUS]/[SLOP]) — no blocking-human legitimacy checkpoint required. |

No high-severity threats. No user input, auth, sessions, or API surface in this phase (ASVS V2-V6 not applicable per RESEARCH Security Domain).
</threat_model>

<verification>
- `npm run build` exits 0.
- `ls public/img/services/*.webp | wc -l` returns 6; each WebP < 200 KB.
- Source PNGs in `src/img/services/` preserved.
- `sharp` removed from package.json devDependencies.
- ServicesPage.jsx: 0 PNG references, 6 `/img/services/*.webp` references, serviceImg1-6 const declarations present, services array imgSrc references unchanged (6).
</verification>

<success_criteria>
- Success Criterion #3 (ROADMAP): all 6 service card image paths resolve to WebP files — no PNG 404s, no fallback to uncompressed sources.
- FOUN-02 satisfied.
- Image payload reduced from ~12 MB of PNGs to < 600 KB of WebP across the 6 cards.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-02-SUMMARY.md` when done. Record: per-file WebP sizes (and total vs original PNG total), confirmation source PNGs were preserved, and confirmation that sharp was uninstalled.
</output>
