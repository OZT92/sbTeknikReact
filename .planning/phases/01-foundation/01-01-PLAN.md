---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.css
  - index.html
  - public/fonts/Inter/Inter-VariableFont_opsz,wght.woff2
  - public/fonts/BarlowCondensed/BarlowCondensed-Bold.woff2
  - public/fonts/BarlowCondensed/BarlowCondensed-Black.woff2
  - scripts/convert-inter.mjs
  - package.json
autonomous: false
requirements: [FOUN-01, FOUN-03, FOUN-04]
user_setup: []

must_haves:
  truths:
    - "Opening the site in a browser shows the dark ground background (#0f1923) applied globally via body — no light flash"
    - "Inter loads as WOFF2 — no 854 KB TTF request appears in the DevTools Network tab"
    - "Barlow Condensed 700 and 900 are self-hosted as WOFF2 and available as the display font"
    - "The :root token block is the first CSS rule in src/index.css with color, spacing, type, z-index, and motion tokens defined"
  artifacts:
    - path: "src/index.css"
      provides: ":root token block, updated Inter @font-face (WOFF2), two Barlow Condensed @font-face blocks, token-driven body rule"
      contains: ":root"
    - path: "public/fonts/Inter/Inter-VariableFont_opsz,wght.woff2"
      provides: "Inter variable font compressed to WOFF2 (~200 KB, <400 KB)"
    - path: "public/fonts/BarlowCondensed/BarlowCondensed-Bold.woff2"
      provides: "Barlow Condensed weight 700 self-hosted display font"
    - path: "public/fonts/BarlowCondensed/BarlowCondensed-Black.woff2"
      provides: "Barlow Condensed weight 900 self-hosted display font"
    - path: "index.html"
      provides: "Preload hints for Inter WOFF2 and Barlow Condensed Black WOFF2 with crossorigin"
      contains: "as=\"font\""
  key_links:
    - from: "src/index.css @font-face Inter"
      to: "public/fonts/Inter/Inter-VariableFont_opsz,wght.woff2"
      via: "src: url() format(woff2)"
      pattern: "Inter-VariableFont.*\\.woff2.*format\\(\"woff2\"\\)"
    - from: "src/index.css body rule"
      to: ":root --color-bg"
      via: "background-color: var(--color-bg)"
      pattern: "background-color:\\s*var\\(--color-bg\\)"
    - from: "index.html preload"
      to: "public/fonts/*.woff2"
      via: "<link rel=preload as=font crossorigin>"
      pattern: "rel=\"preload\".*as=\"font\".*crossorigin"
---

<objective>
Build the dark-ground + fonts vertical slice of the Foundation walking skeleton. After this plan, opening the site in a browser shows the dark industrial background globally, Inter loads as a compressed WOFF2 (no 854 KB TTF request), and Barlow Condensed 700/900 are self-hosted and ready as the display font. The complete `:root` token system that every later phase inherits is in place.

Purpose: Establishes the design-system base and the fast-asset font pipeline. Prevents dark-design patchiness (set the ground globally before any component work) and removes the largest single load-time blocker (the TTF).
Output: Updated `src/index.css` (`:root` block + 3 `@font-face` + token body rule), 3 WOFF2 font files in `public/fonts/`, deleted `public/fonts/Inter/static/`, preload hints in `index.html`, one-off conversion script, cleaned `package.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-RESEARCH.md
@CLAUDE.md
@src/index.css
@index.html
@public/sbLogo.png

<interfaces>
<!-- Token names are a cross-phase contract. Phases 2-5 reference these EXACT property names. -->
<!-- Do not rename, reorder semantically, or alter values without breaking downstream inheritance. -->

Required :root custom properties (from UI-SPEC, verbatim values):
  Colors:    --color-bg #0f1923, --color-surface #1a2840, --color-surface-raised #253347,
             --color-accent #c8960c, --color-accent-hover #e8b010, --color-text-primary #f0ebe4,
             --color-text-muted #7a8fa8, --color-border #253347, --color-border-subtle #1e2e42,
             --color-destructive #c0392b, --color-white #ffffff, --color-overlay rgba(15,25,35,0.72)
  Fonts:     --font-display 'Barlow Condensed', sans-serif; --font-body 'Inter', sans-serif
  Type:      --text-body 1.6rem, --text-label 1.4rem, --text-heading 2.4rem,
             --text-display-lg clamp(4rem, 6vw, 9rem)
  Spacing:   --space-xs 0.4rem, --space-sm 0.8rem, --space-md 1.6rem, --space-lg 2.4rem,
             --space-xl 3.2rem, --space-2xl 4.8rem, --space-3xl 6.4rem, --space-4xl 12.8rem
  Z-index:   --z-base 0, --z-raised 10, --z-overlay 100, --z-float 200, --z-navbar 300
  Motion:    --ease-out cubic-bezier(0.16,1,0.3,1), --duration-fast 150ms,
             --duration-normal 300ms, --duration-slow 500ms

The complete copy-paste-ready :root block, @font-face blocks, and body rule are in
01-PATTERNS.md (lines 70-174) and 01-UI-SPEC.md (Font Loading Strategy + :root Token Block Structure).
Barlow Condensed CDN URLs and curl commands are in 01-RESEARCH.md Pattern 6.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert Inter TTF to WOFF2, download Barlow Condensed, delete static/</name>
  <files>scripts/convert-inter.mjs, public/fonts/Inter/Inter-VariableFont_opsz,wght.woff2, public/fonts/BarlowCondensed/BarlowCondensed-Bold.woff2, public/fonts/BarlowCondensed/BarlowCondensed-Black.woff2, package.json</files>
  <read_first>
    - .planning/phases/01-foundation/01-PATTERNS.md (scripts/convert-inter.mjs section, lines 262-281 — verbatim script)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 6, lines 321-335 — Barlow Condensed curl commands and URLs; Pitfall 1, 2, 5)
    - .planning/phases/01-foundation/01-UI-SPEC.md (Font Loading Strategy, lines 172-245)
    - package.json (confirm "type": "module" and current devDependencies)
  </read_first>
  <action>
    Install temporary dev dependency: run `npm install --save-dev ttf2woff2`.
    Create `scripts/convert-inter.mjs` using the verbatim ESM script in 01-PATTERNS.md lines 269-277: it reads `public/fonts/Inter/Inter-VariableFont_opsz,wght.ttf`, runs `ttf2woff2`, and writes `public/fonts/Inter/Inter-VariableFont_opsz,wght.woff2`. Run it with `node scripts/convert-inter.mjs`.
    Verify the output WOFF2 is between 100 KB and 400 KB (expected ~180-220 KB) using `du -h`. If it exceeds 400 KB, the conversion failed (Pitfall 1) — stop and report; do not proceed.
    Create directory `public/fonts/BarlowCondensed/` and curl-download both WOFF2 files from the Google Fonts CDN URLs in 01-RESEARCH.md Pattern 6 (lines 329-333): `BarlowCondensed-Bold.woff2` (weight 700) and `BarlowCondensed-Black.woff2` (weight 900). Use `curl -L -w "%{http_code}"` and confirm HTTP 200 plus file size > 1 KB for each. If either returns 404 (Pitfall 2), re-query `https://fonts.googleapis.com/css2?family=Barlow+Condensed:wght@700;900&display=swap` with a desktop User-Agent header, extract the current WOFF2 URLs from the CSS response, and retry.
    Delete the entire `public/fonts/Inter/static/` directory (54 unused TTF files, Pitfall 5) with `rm -rf public/fonts/Inter/static`. Before deleting, confirm no `/fonts/Inter/static/` reference exists in `src/index.css` (research confirms none — re-verify with grep).
    After conversions are committed and verified, remove `ttf2woff2` from devDependencies: run `npm uninstall ttf2woff2`. Keep the script file (committed as a reproducible utility) but the package is removed to keep devDependencies minimal.
    Do NOT delete the source `Inter-VariableFont_opsz,wght.ttf` — it is the source of record.
  </action>
  <verify>
    <automated>ls "public/fonts/Inter/Inter-VariableFont_opsz,wght.woff2" && ls public/fonts/BarlowCondensed/BarlowCondensed-Bold.woff2 && ls public/fonts/BarlowCondensed/BarlowCondensed-Black.woff2 && test ! -d public/fonts/Inter/static && test $(stat -f%z "public/fonts/Inter/Inter-VariableFont_opsz,wght.woff2") -lt 409600</automated>
  </verify>
  <acceptance_criteria>
    - `ls "public/fonts/Inter/Inter-VariableFont_opsz,wght.woff2"` exits 0
    - WOFF2 file size is < 409600 bytes (400 KB): `test $(stat -f%z "public/fonts/Inter/Inter-VariableFont_opsz,wght.woff2") -lt 409600` exits 0
    - `ls public/fonts/BarlowCondensed/BarlowCondensed-Bold.woff2` exits 0 and file is > 1 KB
    - `ls public/fonts/BarlowCondensed/BarlowCondensed-Black.woff2` exits 0 and file is > 1 KB
    - `test ! -d public/fonts/Inter/static` exits 0 (directory deleted)
    - `ls "public/fonts/Inter/Inter-VariableFont_opsz,wght.ttf"` exits 0 (source TTF preserved)
    - `grep -q '"ttf2woff2"' package.json` exits 1 (ttf2woff2 removed from package.json)
    - `ls scripts/convert-inter.mjs` exits 0
  </acceptance_criteria>
  <done>Inter WOFF2 (<400 KB) and both Barlow Condensed WOFF2 files exist in public/fonts/; static/ directory deleted; source TTF preserved; ttf2woff2 removed from devDependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Prepend :root token block, update @font-face and body, add preload hints</name>
  <files>src/index.css, index.html</files>
  <read_first>
    - src/index.css (lines 1-26 — current @font-face, reset, html, body rules that are being prepended above / updated)
    - .planning/phases/01-foundation/01-PATTERNS.md (src/index.css section lines 25-176 — exact injection order, verbatim :root block, @font-face blocks, body rule; index.html section lines 236-258)
    - .planning/phases/01-foundation/01-UI-SPEC.md (:root Token Block Structure lines 302-313; Global Body Styles lines 285-298; Font Loading Strategy preload hints lines 236-243)
    - index.html (lines 1-20 — existing preload hint pattern at lines 8-13)
  </read_first>
  <action>
    In `src/index.css`, prepend the complete `:root` block as the FIRST CSS rule (above the existing `@font-face`), using the verbatim block in 01-PATTERNS.md lines 72-121. Include the top comment `/* 1rem = 10px (html font-size: 62.5%) */`. Group order: colors, font families, type scale, spacing, z-index, motion. All token names and values must match the `<interfaces>` contract exactly (per FOUN-03).
    Update the existing Inter `@font-face` (current lines 4-8) to use `src: url("/fonts/Inter/Inter-VariableFont_opsz,wght.woff2") format("woff2")`, add `font-weight: 100 900` and `font-style: normal`, keep `font-display: swap` (per FOUN-01). Do NOT add a unicode-range to Inter (it already covers full Latin + Turkish as a variable font).
    Add two new `@font-face` blocks for "Barlow Condensed" — weight 700 pointing at BarlowCondensed-Bold.woff2 and weight 900 pointing at BarlowCondensed-Black.woff2 — each with `font-display: swap` and the Turkish `unicode-range` block verbatim from 01-PATTERNS.md lines 138-162 (per FOUN-04).
    Update the `body` rule to declare exactly: `font-family: var(--font-body)`, `background-color: var(--color-bg)`, `color: var(--color-text-primary)`, `overflow-x: hidden` — replacing the literal `#fefcfb` and `"Inter", sans-serif`. Do NOT change any other body property, and do NOT touch the `* {}` reset, `html { font-size: 62.5% }`, or any rule from old line 26 onward (lines 26-853 are Phase 2-5 work — see RESEARCH "Existing CSS State").
    In `index.html`, add two `<link rel="preload" ... as="font" type="font/woff2" crossorigin>` hints after the existing poster-hero preload (line 13), for `/fonts/Inter/Inter-VariableFont_opsz,wght.woff2` and `/fonts/BarlowCondensed/BarlowCondensed-Black.woff2`, verbatim from 01-PATTERNS.md lines 252-255. The `crossorigin` attribute is mandatory — without it the browser silently discards the font preload (Pitfall 6).
  </action>
  <verify>
    <automated>npm run build && test $(grep -c "^:root" src/index.css) -eq 1 && grep -q 'Inter-VariableFont_opsz,wght.woff2") format("woff2")' src/index.css && test $(grep -c "Barlow Condensed" src/index.css) -ge 2 && grep -q "background-color: var(--color-bg)" src/index.css && test $(grep 'as="font"' index.html | grep -c crossorigin) -eq 2</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 (no compile/import errors)
    - `grep -c "^:root" src/index.css` returns exactly 1
    - `grep "\-\-color-bg" src/index.css` exits 0
    - `grep "\-\-space-4xl" src/index.css` exits 0 (full token block, not partial)
    - `grep "\-\-z-navbar" src/index.css` exits 0
    - `grep "\-\-ease-out" src/index.css` exits 0
    - `grep 'Inter-VariableFont_opsz,wght.woff2") format("woff2")' src/index.css` exits 0 (TTF reference gone)
    - `grep -c "Barlow Condensed" src/index.css` returns ≥ 2 (two @font-face blocks)
    - `grep "background-color: var(--color-bg)" src/index.css` exits 0
    - `grep "color: var(--color-text-primary)" src/index.css` exits 0
    - `grep "font-size: 62.5%" src/index.css` exits 0 (html rule preserved untouched)
    - `grep 'as="font"' index.html | grep -c crossorigin` returns 2
  </acceptance_criteria>
  <done>:root token block is the first rule in src/index.css with all token groups; Inter @font-face points at WOFF2 with weight 100 900; two Barlow Condensed @font-face blocks with Turkish unicode-range; body uses var(--color-bg) + var(--color-text-primary); index.html has two crossorigin font preloads; npm run build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Validate logo color and dark-ground walking skeleton in browser</name>
  <action>Pause for human verification (this is the one manual step in the plan, per VALIDATION.md manual-only verifications and STATE.md risk #1). The executor runs `npm run dev`, the human confirms the dark-ground background and WOFF2 font loading in the browser per the steps in how-to-verify, and validates the amber accent token (--color-accent: #c8960c) against public/sbLogo.png. If the accent clashes with the logo, the executor updates --color-accent and --color-accent-hover in the :root block of src/index.css and re-runs `npm run build` before this checkpoint is resolved. Do not start Phase 2 until this checkpoint is approved.</action>
  <files>src/index.css</files>
  <what-built>
    The :root token block is live, Inter serves as WOFF2, Barlow Condensed 700/900 are self-hosted, the body uses the dark-ground tokens, and font preload hints are in index.html. This is the dark-ground + fonts half of the walking skeleton. The amber accent token (--color-accent: #c8960c) is set in :root but has NOT yet been validated against the actual company logo — that validation is part of this checkpoint and gates all Phase 2+ accent styling.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open the local URL in a browser.
    2. Confirm the page background is the dark industrial ground (#0f1923) on every route (/, /about, /services, /contact) — no white/light background, no white flash on hard refresh (use DevTools "Disable cache" + reload).
    3. Open DevTools → Network tab, filter by "Font", reload. Confirm Inter loads as `Inter-VariableFont_opsz,wght.woff2` and there is NO `.ttf` request (no 854 KB TTF). Confirm both Barlow Condensed `.woff2` files load. Confirm the two font preload hints are used (no "preload not used / credentials mismatch" warning in Console).
    4. Open `public/sbLogo.png` and visually compare the amber accent #c8960c (you can preview the swatch in DevTools by inspecting an element and setting a temporary background) against the logo's primary colors. Decide: does amber #c8960c harmonize with the logo, or does it clash?
       - If it harmonizes: approve as-is.
       - If it clashes: tell the executor the corrected hex values, and the executor will update `--color-accent` and `--color-accent-hover` in the :root block and re-run `npm run build` before this checkpoint is resolved. This MUST be settled before Phase 2 begins (STATE.md risk #1, RESEARCH assumption A1).
  </how-to-verify>
  <verify><human-check>Dark background visible on all 4 routes with no white flash; no .ttf request in DevTools Network tab; both Barlow Condensed WOFF2 files load; amber #c8960c validated against public/sbLogo.png (kept or corrected).</human-check></verify>
  <resume-signal>Type "approved" if dark background + WOFF2 fonts are correct and amber #c8960c works with the logo. Otherwise describe the issue (e.g. "white flash on /about" or "accent should be #b8851a") and the executor will fix it.</resume-signal>
  <done>Human has confirmed the dark-ground walking skeleton in the browser (dark background on all routes, WOFF2 fonts, no TTF request) and the amber accent is validated or corrected against the logo. Checkpoint approved before Phase 2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local build → external CDN (fonts.gstatic.com) | Barlow Condensed WOFF2 binaries are downloaded over the network into the repo |
| Local build → npm registry | `ttf2woff2` is installed and its native/JS code runs the conversion |
| public/ directory → production dist/ | Everything in public/ is copied verbatim into the deployed bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Barlow Condensed WOFF2 downloaded from fonts.gstatic.com via curl | accept | Source is Google's official font CDN over HTTPS (TLS authenticates origin). WOFF2 is non-executable binary font data with no script surface. URLs confirmed via Google Fonts CSS API. No integrity hash required for self-hosted local files (SRI applies to remote `<link>`/`<script>`, not local assets). Task verifies HTTP 200 + size > 1 KB to reject truncated/error responses. |
| T-01-02 | Tampering | `ttf2woff2` npm install + conversion script | mitigate | Package vetted in RESEARCH Package Legitimacy Audit (408K wk downloads, official repo nfroidure/ttf2woff2, 11-yr history, no postinstall network calls). Installed as temporary devDependency and uninstalled after conversion. Output WOFF2 size-gated (<400 KB) to detect corrupt/unexpected output. |
| T-01-03 | Information Disclosure | Deleting public/fonts/Inter/static/ (54 TTFs) | accept | Pure cleanup of unused assets; reduces dist/ bloat. Grep-confirmed no CSS references static/. Source variable TTF preserved. No sensitive data involved. |
| T-01-SC | Tampering | npm install (ttf2woff2) | mitigate | RESEARCH Package Legitimacy Audit present and complete. ttf2woff2 disposition Approved (not [ASSUMED]/[SUS]/[SLOP]) after manual verification of registry, downloads, source repo, and install-script behavior — no blocking-human legitimacy checkpoint required. |

No high-severity threats. No user input, auth, sessions, or API surface in this phase (ASVS V2-V6 not applicable per RESEARCH Security Domain).
</threat_model>

<verification>
- `npm run build` exits 0.
- `ls "public/fonts/Inter/Inter-VariableFont_opsz,wght.woff2"` and both Barlow Condensed WOFF2 files exist; Inter WOFF2 < 400 KB.
- `public/fonts/Inter/static/` no longer exists; source Inter TTF preserved.
- `grep -c "^:root" src/index.css` returns 1; all token groups present (--color-bg, --space-4xl, --z-navbar, --ease-out).
- Inter @font-face references WOFF2 with `format("woff2")`; no TTF reference remains in the Inter @font-face.
- Two "Barlow Condensed" @font-face blocks present with Turkish unicode-range.
- body uses `var(--color-bg)` + `var(--color-text-primary)`; `html { font-size: 62.5% }` untouched.
- index.html has two `as="font"` preloads, both with `crossorigin`.
- Human checkpoint: dark background visible on all 4 routes with no white flash; no `.ttf` request in Network tab; logo amber validated/corrected.
</verification>

<success_criteria>
- Success Criterion #1 (ROADMAP): dark ground background applied globally via body — confirmed in browser, no light flash.
- Success Criterion #2 (ROADMAP): Barlow Condensed 700/900 + Inter load as WOFF2 with no 854 KB TTF request in Network tab.
- Success Criterion #4 (ROADMAP): :root token block present at the top of src/index.css with color, spacing, and typography custom properties.
- FOUN-01, FOUN-03, FOUN-04 satisfied.
- Logo color validation (STATE.md risk #1 / RESEARCH A1) resolved before Phase 2.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` when done. Record: final Inter WOFF2 size, confirmed Barlow Condensed URLs used (and whether a 404 fallback re-query was needed), the resolved amber accent value (kept #c8960c or corrected), and confirmation that ttf2woff2 was uninstalled.
</output>
