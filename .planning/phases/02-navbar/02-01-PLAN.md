---
phase: 02-navbar
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.css
  - src/components/Navbar.jsx
autonomous: true
requirements: [NAV-01, NAV-03]
must_haves:
  truths:
    - "On viewports ≥640px the navbar renders with a dark #0f1923 background, not the old semi-transparent #fefcfb81"
    - "No #194d88 corporate-blue value remains anywhere in the navbar CSS rules or Navbar.jsx"
    - "Nav links use Barlow Condensed (--font-display) and the idle text color is muted (#7a8fa8), turning accent gold (#c8960c) on hover"
    - "The current-route link shows accent gold text plus a 2px accent bottom border (active state)"
    - "The navbar sticks to the top of the viewport on scroll"
  artifacts:
    - path: "src/index.css"
      provides: "--text-subheading token + rewritten .navbar desktop/tablet rules using design tokens"
      contains: "--text-subheading"
    - path: "src/components/Navbar.jsx"
      provides: "Semantic <nav> + NavLink-based navbar markup with active-state className callback"
      contains: "NavLink"
  key_links:
    - from: "src/components/Navbar.jsx"
      to: "src/index.css"
      via: "BEM class names navbar__link / navbar__link--active"
      pattern: "navbar__link"
    - from: "src/index.css .navbar"
      to: ":root tokens"
      via: "var() references"
      pattern: "var\\(--color-bg\\)"
---

<objective>
Redesign the desktop and tablet (≥640px) navbar to the bold/industrial aesthetic using the Phase 1 token layer, and convert the markup to semantic `<nav>` + React Router `NavLink` so active-route styling works.

Purpose: This is the first vertical slice of Phase 2 — it delivers a complete, visible, working redesigned navbar on every screen ≥640px (the desktop/tablet experience procurement managers see), with correct typography, color, sticky positioning, hover, and active states. The mobile hamburger drawer is the second slice (Plan 02).

Output: A new `--text-subheading: 2.0rem` token, a fully rewritten navbar CSS block (tokens only, no `#194d88`, no `#fefcfb81`), and a `NavLink`-based `Navbar.jsx` with active-state styling.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-navbar/02-UI-SPEC.md
@CLAUDE.md

<interfaces>
<!-- Tokens that ALREADY exist in :root (src/index.css lines 1-50). Use these directly; do not redefine. -->
Existing :root tokens (src/index.css):
  --color-bg: #0f1923;            --color-surface: #1a2840;
  --color-accent: #c8960c;        --color-accent-hover: #e8b010;
  --color-text-primary: #f0ebe4;  --color-text-muted: #7a8fa8;
  --color-border: #253347;
  --font-display: 'Barlow Condensed', sans-serif;
  --text-body: 1.6rem;  --text-label: 1.4rem;  --text-heading: 2.4rem;
  --space-xs: 0.4rem; --space-sm: 0.8rem; --space-md: 1.6rem;
  --space-lg: 2.4rem; --space-xl: 3.2rem;
  --z-navbar: 300;
  --ease-out: cubic-bezier(0.16, 1, 0.3, 1);  --duration-fast: 150ms;

NOTE: --text-subheading (2.0rem) does NOT exist yet — Task 1 adds it.

<!-- React Router NavLink className callback contract (react-router 7.12.0, already installed) -->
NavLink accepts: className={({ isActive }) => isActive ? "navbar__link navbar__link--active" : "navbar__link"}
NavLink sets aria-current="page" on the active link automatically.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add --text-subheading token and rewrite navbar CSS for ≥640px using design tokens</name>
  <files>src/index.css</files>
  <read_first>
    - src/index.css (read the full :root block lines 1-50 to confirm token names, and the NAVBAR block lines 106-162 plus the ≥640px / ≥1024px navbar rules at lines 647-668 and 709-726 — these are the rules you will replace)
    - .planning/phases/02-navbar/02-UI-SPEC.md (sections: Typography, Color, Component Anatomy, Hover and Active States, Scroll Behavior — these are the locked values, do NOT invent any value not in the spec)
  </read_first>
  <action>
    In the `:root` block of src/index.css, add a new token immediately after the `--text-label: 1.4rem;` line in the Type Scale group:
    `--text-subheading: 2.0rem;`

    Replace the entire NAVBAR CSS block (the rules `.navbar`, `.navbar-logo`, `.nav-logo-container`, `.nav-logo-container::after`, `.nav-logo-text`, `.navbar-links`, `.navbar-links_link`, `.navbar-links_link:hover`) with new token-based rules. Rename classes to BEM kebab-case as follows: `.navbar` stays `.navbar`; logo link wrapper becomes `.navbar__logo-link`; logo image becomes `.navbar__logo-img`; logo wordmark becomes `.navbar__wordmark`; links container becomes `.navbar__links`; each link becomes `.navbar__link`; the link text becomes `.navbar__link-text`. Add `.navbar__link--active` for active state.

    Base (mobile-first, applies to all viewports) `.navbar` rule must set EXACTLY:
    - `background-color: var(--color-bg);`
    - `backdrop-filter: blur(8px);`
    - `-webkit-backdrop-filter: blur(8px);`
    - `display: flex; justify-content: space-between; align-items: center;`
    - `width: 100%;`
    - `position: sticky; top: 0; z-index: var(--z-navbar);`
    - `border-bottom: 1px solid var(--color-border);`
    - `padding: 0 var(--space-md);` (16px mobile horizontal padding)
    - `height: 56px;` (mobile bar height)

    `.navbar__logo-link` rule: `position: relative; display: flex; align-items: center; gap: var(--space-sm); text-decoration: none;`
    `.navbar__logo-img` rule: `height: 56px; width: auto;` (mobile height)
    `.navbar__logo-link::after` rule (the divider): `content: ""; width: 0; height: 40px; border-right: 1px solid var(--color-accent); margin: 0 var(--space-sm); transition: border-color var(--duration-fast) var(--ease-out);`
    `.navbar__wordmark` rule: `font-family: var(--font-display); font-weight: 900; font-size: var(--text-label); letter-spacing: 0.04em; line-height: 1; text-transform: uppercase; color: var(--color-text-primary); transition: color var(--duration-fast) var(--ease-out);`
    `.navbar__logo-link:hover .navbar__wordmark` rule: `color: var(--color-accent-hover);`
    `.navbar__logo-link:hover::after` rule: `border-color: var(--color-accent-hover);`

    `.navbar__links` rule: `display: flex; align-items: center; gap: var(--space-md);`
    `.navbar__link` rule: `font-family: var(--font-display); font-weight: 700; font-size: var(--text-label); letter-spacing: 0.06em; line-height: 1; text-decoration: none; color: var(--color-text-muted); display: flex; align-items: center; gap: var(--space-xs); transition: color var(--duration-fast) var(--ease-out); padding-bottom: 2px; border-bottom: 2px solid transparent;`
    `.navbar__link:hover` rule: `color: var(--color-accent);`
    `.navbar__link--active` rule: `color: var(--color-accent); border-bottom-color: var(--color-accent);`
    `.navbar__link:focus-visible` rule: `outline: 2px solid var(--color-accent); outline-offset: 3px;`

    Update the ≥640px (tablet) navbar media-query rules at src/index.css lines 647-668: replace the old `.navbar-logo`, `.nav-logo-container::after`, `.nav-logo-text`, `.navbar-links`, `.navbar-links_link` overrides with new-class equivalents:
    - `.navbar { height: 64px; padding: 0 var(--space-lg); }`
    - `.navbar__logo-img { height: 64px; }`
    - `.navbar__wordmark { font-size: var(--text-body); }`
    - `.navbar__links { gap: var(--space-lg); }`
    - `.navbar__link { font-size: var(--text-body); letter-spacing: 0.08em; }`

    Update the ≥1024px (desktop) navbar media-query rules at src/index.css lines 709-726: replace old-class overrides with:
    - `.navbar { height: 72px; }`
    - `.navbar__logo-img { height: 72px; }`
    - `.navbar__wordmark { font-size: var(--text-heading); }`
    - `.navbar__link { font-size: var(--text-subheading); }`

    Do NOT leave any rule referencing `.navbar-logo`, `.nav-logo-container`, `.nav-logo-text`, `.navbar-links`, `.navbar-links_link`, `.navbar-link-text`, `#194d88`, or `#fefcfb81`. Do NOT modify any non-navbar rule (hero, services, contact, about CSS stays untouched in this plan).
  </action>
  <verify>
    <automated>cd /Users/ozt92/Documents/development/sbteknikv2/sbTeknikReact && grep -c "\-\-text-subheading" src/index.css && grep -v '^\s*/\*' src/index.css | grep -ic "194d88\|fefcfb81\|navbar-links_link\|nav-logo-text" | grep -qx 0 && echo "NO_OLD_NAV_REFS"</automated>
  </verify>
  <done>
    src/index.css contains `--text-subheading: 2.0rem`; the navbar CSS block uses only token var() references and BEM `.navbar__*` class names; zero occurrences of `#194d88`, `#fefcfb81`, or any old navbar class name remain.
  </done>
  <acceptance_criteria>
    - `grep -c "\-\-text-subheading: 2.0rem" src/index.css` returns 1
    - `grep -c "\.navbar__link--active" src/index.css` returns at least 1
    - `grep -c "var(--color-bg)" src/index.css` includes the `.navbar` background (navbar rule no longer contains `#fefcfb81`)
    - `grep -c "194d88" src/index.css` returns 0
    - `grep -c "fefcfb81" src/index.css` returns 0
    - `grep -c "navbar-links_link\|nav-logo-text\|nav-logo-container" src/index.css` returns 0
    - `grep -c "position: sticky" src/index.css` returns at least 1 (navbar sticky)
    - `grep -c "backdrop-filter: blur(8px)" src/index.css` returns at least 1
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Convert Navbar.jsx to semantic nav + NavLink with active-state className and new BEM classes</name>
  <files>src/components/Navbar.jsx</files>
  <read_first>
    - src/components/Navbar.jsx (read the full current file — current markup uses `<div className="navbar">`, plain `<Link>`, old classes `nav-logo-container`, `nav-logo-text`, `navbar-links`, `navbar-links_link`, `navbar-link-text`)
    - src/index.css (read the navbar CSS block AS REWRITTEN by Task 1 so the JSX class names match exactly: `.navbar`, `.navbar__logo-link`, `.navbar__logo-img`, `.navbar__wordmark`, `.navbar__links`, `.navbar__link`, `.navbar__link-text`, `.navbar__link--active`)
    - .planning/phases/02-navbar/02-UI-SPEC.md (sections: Copywriting Contract, Accessibility, Typography — for exact Turkish copy, aria labels, and icon sizes)
  </read_first>
  <action>
    Rewrite src/components/Navbar.jsx. Change the import on line 1 from `import { Link } from "react-router";` to `import { NavLink, Link } from "react-router";` (keep `Link` import — the logo wrapper stays a plain `Link`, only the nav items become `NavLink`).

    Replace the root `<div className="navbar">` with `<nav className="navbar" aria-label="Ana navigasyon">` and close it with `</nav>`.

    Logo block: wrap the logo image + wordmark in `<Link className="navbar__logo-link" to="/" aria-label="Ana Sayfa">`. The image keeps `src={sbLogo}` with `className="navbar__logo-img"` and `alt="SB Teknik logosu"`. Replace the `<p className="nav-logo-text">SB Teknik Malzeme</p>` with `<span className="navbar__wordmark">SB Teknik Malzeme</span>`.

    Links container: `<div className="navbar__links">`. Each link becomes a `NavLink` with the active-aware className callback:
    `className={({ isActive }) => isActive ? "navbar__link navbar__link--active" : "navbar__link"}`
    - Link 1: `to="/about"`, icon `<InfoIcon size={20} aria-hidden="true" />`, text `<span className="navbar__link-text">Hakkımızda</span>`
    - Link 2: `to="/services"`, icon `<PackageIcon size={20} aria-hidden="true" />`, text `<span className="navbar__link-text">Hizmetlerimiz</span>`
    - Link 3: `to="/contact"`, icon `<MapPinLineIcon size={20} aria-hidden="true" />`, text `<span className="navbar__link-text">İletişim</span>`

    Keep the existing Phosphor icon imports (`InfoIcon, PackageIcon, MapPinLineIcon`) and the `sbLogo` import. Remove the stray `{" "}` whitespace JSX expressions that exist in the current file. Do NOT add inline event handlers, `dangerouslySetInnerHTML`, or any hamburger/state logic — mobile drawer is Plan 02. Component stays a stateless function returning JSX.
  </action>
  <verify>
    <automated>cd /Users/ozt92/Documents/development/sbteknikv2/sbTeknikReact && grep -q "NavLink" src/components/Navbar.jsx && grep -q "aria-label=\"Ana navigasyon\"" src/components/Navbar.jsx && grep -qc "194d88" src/components/Navbar.jsx; grep -c "navbar__link--active" src/components/Navbar.jsx && npm run build 2>&1 | tail -1</automated>
  </verify>
  <done>
    Navbar.jsx renders a `<nav aria-label="Ana navigasyon">` containing a `Link` logo wrapper and three `NavLink` items using the active-state className callback; all Phosphor icons have `aria-hidden="true"` and `size={20}`; `npm run build` exits 0.
  </done>
  <acceptance_criteria>
    - `grep -c "NavLink" src/components/Navbar.jsx` returns at least 4 (import + 3 usages)
    - `grep -c "navbar__link--active" src/components/Navbar.jsx` returns 3
    - `grep -c 'aria-label="Ana navigasyon"' src/components/Navbar.jsx` returns 1
    - `grep -c 'aria-label="Ana Sayfa"' src/components/Navbar.jsx` returns 1
    - `grep -c 'aria-hidden="true"' src/components/Navbar.jsx` returns 3
    - `grep -c "size={20}" src/components/Navbar.jsx` returns 3
    - `grep -c "navbar__wordmark" src/components/Navbar.jsx` returns 1
    - `grep -c "dangerouslySetInnerHTML\|onClick=\|onMouseOver=" src/components/Navbar.jsx` returns 0
    - `npm run build` exits 0
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Desktop/tablet (≥640px) navbar redesigned to the industrial aesthetic: dark `#0f1923` background with blur, Barlow Condensed links, muted idle text turning gold on hover, gold active-route underline, sticky positioning, accent logo divider. Old `#194d88` corporate blue and `#fefcfb81` background fully removed. Markup converted to semantic `<nav>` + `NavLink`.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open the printed localhost URL in a browser at desktop width (≥1024px).
    2. Confirm the navbar background is dark (`#0f1923`), not light/translucent.
    3. Confirm nav link text is in the condensed display font and sits in a muted blue-grey when idle.
    4. Hover a non-active link — text should turn gold (`#c8960c`), color-only, no underline-on-hover, no background change.
    5. Navigate to /about, /services, /contact — the current page's link should show gold text plus a 2px gold underline (active state).
    6. Scroll the page — navbar should stick to the top.
    7. Hover the logo — wordmark and divider line should shift to brighter gold (`#e8b010`).
    8. Resize from 1024px down to 640px — links stay horizontal, sizes step down, no layout breakage. (Below 640px the layout is incomplete — that is expected; Plan 02 adds the hamburger.)
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none (static read-only) | This phase ships a static, read-only navbar. No user input, no API calls, no auth, no PII. The only "boundary" is the build output served to browsers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering (XSS) | Navbar.jsx markup | mitigate | Use React synthetic JSX only — no inline DOM event-handler attributes, no `dangerouslySetInnerHTML`. All copy is static literals. Acceptance criteria greps for `dangerouslySetInnerHTML`/`onClick=`/`onMouseOver=` returning 0. |
| T-02-02 | Information Disclosure | Navbar.jsx / index.css | accept | No secrets, credentials, or env vars in navbar code. Low-value static marketing surface; nothing to disclose. |
| T-02-SC | Tampering (supply chain) | npm installs | accept | No new packages installed this phase — NavLink (react-router 7.12.0), motion/react, @phosphor-icons/react all already in package.json. No install task exists, so no legitimacy gate required. |
</threat_model>

<verification>
- `npm run build` exits 0 with the rewritten navbar.
- `grep -c "194d88" src/index.css src/components/Navbar.jsx` returns 0 across both files.
- `--text-subheading: 2.0rem` present in `:root`.
- Active-state styling driven by `NavLink` + `.navbar__link--active`.
- Human visual checkpoint confirms dark background, gold hover/active, sticky behavior at ≥640px.
</verification>

<success_criteria>
- NAV-01 (partial, ≥640px): navbar visually redesigned to industrial aesthetic using token layer — dark bg, Barlow Condensed, new color system.
- NAV-03: hover state = accent gold; active state = accent gold text + 2px accent underline; old corporate-blue highlight removed.
- NAV-02 (partial): tablet/desktop breakpoints (640px, 1024px) adapt cleanly. Mobile <640px completed in Plan 02.
- Old `#194d88` and `#fefcfb81` fully removed from navbar.
- `--text-subheading: 2.0rem` token added.
</success_criteria>

<output>
Create `.planning/phases/02-navbar/02-01-SUMMARY.md` when done.
</output>
