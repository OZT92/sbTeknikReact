---
phase: 02-navbar
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/Navbar.jsx
  - src/index.css
autonomous: false
requirements: [NAV-02, NAV-03]
must_haves:
  truths:
    - "Below 640px the horizontal nav links are hidden and a 44×44px hamburger button is shown"
    - "Tapping the hamburger opens a drawer (slides down from below the bar) listing the three nav links; tapping again (or the X icon) closes it"
    - "The drawer animates with motion/react spring on open and reverses on close, but shows/hides instantly when prefers-reduced-motion is set"
    - "The active route in the mobile drawer shows a 3px accent left border plus accent text"
    - "At ≥640px the hamburger is hidden and the horizontal links from Plan 01 are shown — no regression"
  artifacts:
    - path: "src/components/Navbar.jsx"
      provides: "useState toggle, hamburger button, AnimatePresence + motion.div drawer, useReducedMotion guard"
      contains: "AnimatePresence"
    - path: "src/index.css"
      provides: "Mobile <640px rules: hide .navbar__links, show .navbar__toggle, drawer styling, drawer active state"
      contains: "navbar__toggle"
  key_links:
    - from: "src/components/Navbar.jsx useState(isOpen)"
      to: ".navbar__drawer AnimatePresence"
      via: "conditional render gated on isOpen"
      pattern: "AnimatePresence"
    - from: "src/components/Navbar.jsx useReducedMotion()"
      to: "motion.div transition"
      via: "duration 0 when reduced motion"
      pattern: "useReducedMotion"
---

<objective>
Add the mobile (<640px) hamburger menu and animated drawer to the navbar, completing responsive behavior across all breakpoints.

Purpose: Second vertical slice of Phase 2. Plan 01 delivered a complete ≥640px navbar; this slice delivers the complete mobile experience — a working hamburger toggle and a sliding drawer with the same active/hover accent system. After this plan a user on any device, from 320px to wide desktop, gets a correct, fully responsive industrial navbar.

Output: `Navbar.jsx` with `useState` toggle, a semantic hamburger `<button>`, an `AnimatePresence` + `motion.div` drawer guarded by `useReducedMotion()`, and the mobile CSS rules that hide horizontal links / show the hamburger / style the drawer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-navbar/02-UI-SPEC.md
@.planning/phases/02-navbar/02-01-PLAN.md
@CLAUDE.md

<interfaces>
<!-- Output of Plan 01 that this plan builds on (read the actual files, these are the contracts) -->
From src/components/Navbar.jsx (after Plan 01):
  - Root element: <nav className="navbar" aria-label="Ana navigasyon">
  - Logo: <Link className="navbar__logo-link" to="/" aria-label="Ana Sayfa"> with .navbar__logo-img + .navbar__wordmark
  - Links container: <div className="navbar__links"> with three NavLink items using:
      className={({ isActive }) => isActive ? "navbar__link navbar__link--active" : "navbar__link"}
  - Imports: NavLink, Link from "react-router"; InfoIcon, PackageIcon, MapPinLineIcon from "@phosphor-icons/react"; sbLogo

From src/index.css (after Plan 01):
  - .navbar has height 56px mobile / 64px tablet / 72px desktop, sticky, var(--color-bg)
  - .navbar__link / .navbar__link--active already styled (gold hover/active)
  - Existing mobile breakpoint pattern: @media (max-width: 640px) { ... } (see .logoSlider block lines 324-334 for the max-width style)
  - Existing min-width breakpoint: @media (min-width: 640px) { ... } (navbar tablet rules live here)

<!-- motion/react 12.34.0 — already installed -->
import { motion, AnimatePresence, useReducedMotion } from "motion/react";
Drawer open variant (from UI-SPEC Animation table):
  initial={{ height: 0, opacity: 0 }} animate={{ height: "auto", opacity: 1 }} exit={{ height: 0, opacity: 0 }}
  transition spring: { type: "spring", stiffness: 300, damping: 30 }
  When useReducedMotion() is true: transition={{ duration: 0 }}

<!-- Phosphor icons for hamburger (List/X) — same library, already installed -->
import { ListIcon, XIcon } from "@phosphor-icons/react";
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add hamburger toggle state, button, and AnimatePresence drawer to Navbar.jsx with reduced-motion guard</name>
  <files>src/components/Navbar.jsx</files>
  <read_first>
    - src/components/Navbar.jsx (read the full file as rewritten by Plan 01 — you must preserve the `<nav>`, logo `Link`, and the three `NavLink` items and their className callback exactly)
    - .planning/phases/02-navbar/02-UI-SPEC.md (sections: Mobile Navbar, Animation, Accessibility, Copywriting Contract — for drawer behavior, spring spec, aria labels, and exact Turkish hamburger labels "Menüyü aç" / "Menüyü kapat")
    - src/index.css (read the navbar block so drawer/toggle class names you add in JSX match the CSS class names added in Task 2)
  </read_first>
  <action>
    Update src/components/Navbar.jsx.

    Imports: add `import { useState } from "react";`. Add `import { motion, AnimatePresence, useReducedMotion } from "motion/react";`. Extend the Phosphor import to also pull `ListIcon, XIcon` alongside the existing icons.

    Inside the component, add state and the reduced-motion flag:
    - `const [isOpen, setIsOpen] = useState(false);`
    - `const reduceMotion = useReducedMotion();`
    - `const toggle = () => setIsOpen((v) => !v);`
    - `const closeMenu = () => setIsOpen(false);`

    Inside the existing `<nav>`, AFTER the `<div className="navbar__links">` block, add the hamburger button (it is CSS-hidden ≥640px in Task 2):
    `<button type="button" className="navbar__toggle" aria-label={isOpen ? "Menüyü kapat" : "Menüyü aç"} aria-expanded={isOpen} onClick={toggle}>` containing an `AnimatePresence` that swaps the icon by key:
    - when `isOpen`: `<motion.span key="x" initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }} transition={{ duration: reduceMotion ? 0 : 0.15 }}><XIcon size={24} aria-hidden="true" /></motion.span>`
    - else: `<motion.span key="list" ...same transition...><ListIcon size={24} aria-hidden="true" /></motion.span>`
    Close the `</button>`.

    AFTER the hamburger button, still inside `<nav>`, add the drawer wrapped in `AnimatePresence`:
    `{isOpen && (` `<motion.nav className="navbar__drawer" aria-label="Mobil menü" initial={{ height: 0, opacity: 0 }} animate={{ height: "auto", opacity: 1 }} exit={{ height: 0, opacity: 0 }} transition={reduceMotion ? { duration: 0 } : { type: "spring", stiffness: 300, damping: 30 }}>` ... `</motion.nav>` `)}` — wrap that whole `{isOpen && (...)}` in `<AnimatePresence>...</AnimatePresence>`.

    Inside the drawer render the same three routes as drawer links, each calling `closeMenu` on click so the menu collapses after navigation:
    `<NavLink to="/about" onClick={closeMenu} className={({ isActive }) => isActive ? "navbar__drawer-link navbar__drawer-link--active" : "navbar__drawer-link"}>Hakkımızda</NavLink>` and likewise `to="/services"` → "Hizmetlerimiz" and `to="/contact"` → "İletişim". (Drawer links are text-only per the UI-SPEC mobile menu spec — no icons required, but you may omit them; keep it text-only for the larger 2.4rem drawer type.)

    Constraints: `onClick={toggle}`/`onClick={closeMenu}` are React synthetic handlers (allowed). Do NOT add `dangerouslySetInnerHTML` or raw DOM inline-attribute handlers. Keep the existing logo `Link` and the desktop `.navbar__links` block from Plan 01 unchanged.
  </action>
  <verify>
    <automated>cd /Users/ozt92/Documents/development/sbteknikv2/sbTeknikReact && grep -q "useReducedMotion" src/components/Navbar.jsx && grep -q "AnimatePresence" src/components/Navbar.jsx && grep -q "navbar__toggle" src/components/Navbar.jsx && grep -q "navbar__drawer" src/components/Navbar.jsx && npm run build 2>&1 | tail -1</automated>
  </verify>
  <done>
    Navbar.jsx has `useState` open/close logic, a `<button className="navbar__toggle">` with toggled `aria-label` and `aria-expanded`, an `AnimatePresence`-wrapped `motion.nav` drawer with the spring transition (or `duration:0` under reduced motion), three drawer `NavLink`s that close the menu on click, and `npm run build` exits 0.
  </done>
  <acceptance_criteria>
    - `grep -c "useState" src/components/Navbar.jsx` returns at least 1
    - `grep -c "useReducedMotion" src/components/Navbar.jsx` returns at least 1 (import + call = 2)
    - `grep -c "AnimatePresence" src/components/Navbar.jsx` returns at least 2 (import + usage)
    - `grep -c "navbar__toggle" src/components/Navbar.jsx` returns 1
    - `grep -c "navbar__drawer-link--active" src/components/Navbar.jsx` returns 3
    - `grep -c "aria-expanded" src/components/Navbar.jsx` returns 1
    - `grep -c '"Menüyü aç"\|"Menüyü kapat"' src/components/Navbar.jsx` returns at least 1
    - `grep -c "stiffness: 300" src/components/Navbar.jsx` returns at least 1
    - `grep -c "dangerouslySetInnerHTML" src/components/Navbar.jsx` returns 0
    - `npm run build` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add mobile <640px CSS — hide horizontal links, show hamburger, style drawer and drawer active state</name>
  <files>src/index.css</files>
  <read_first>
    - src/index.css (read the navbar block as rewritten by Plan 01, and the existing `@media (max-width: 640px)` block at lines ~324-334 and the `@media (min-width: 640px)` navbar rules — you will add to the max-width block and add hamburger-hide to the min-width block)
    - .planning/phases/02-navbar/02-UI-SPEC.md (sections: Mobile Navbar, Hover and Active States, Spacing Scale, Typography — drawer bg = var(--color-surface), drawer link type = --text-heading 2.4rem weight 700, active = 3px left border accent, hamburger hover bg = rgba(200,150,12,0.12))
  </read_first>
  <action>
    Add the following navbar mobile rules to src/index.css.

    Base rule for the hamburger button (visible by default on mobile-first, hidden at ≥640px):
    `.navbar__toggle` rule: `display: flex; align-items: center; justify-content: center; width: 44px; height: 44px; padding: 0; background: transparent; border: none; border-radius: 4px; color: var(--color-text-primary); cursor: pointer; transition: background-color var(--duration-fast) var(--ease-out);`
    `.navbar__toggle:hover, .navbar__toggle:focus-visible` rule: `background-color: rgba(200, 150, 12, 0.12);`
    `.navbar__toggle:focus-visible` rule: `outline: 2px solid var(--color-accent); outline-offset: 3px;`

    Drawer container (mobile-first base; it only renders on mobile because the toggle that opens it is hidden ≥640px):
    `.navbar__drawer` rule: `position: absolute; top: 56px; left: 0; right: 0; display: flex; flex-direction: column; background-color: var(--color-surface); border-bottom: 1px solid var(--color-border); overflow: hidden; z-index: var(--z-navbar);`
    `.navbar__drawer-link` rule: `font-family: var(--font-display); font-weight: 700; font-size: var(--text-heading); letter-spacing: 0.1em; line-height: 1; text-transform: none; text-decoration: none; color: var(--color-text-muted); padding: var(--space-md) var(--space-lg); border-left: 3px solid transparent; transition: color var(--duration-fast) var(--ease-out);`
    `.navbar__drawer-link:hover` rule: `color: var(--color-accent);`
    `.navbar__drawer-link--active` rule: `color: var(--color-accent); border-left-color: var(--color-accent);`

    Inside the EXISTING `@media (max-width: 640px)` block (the one that currently styles `.logoSlider`), add a rule to hide the horizontal links on mobile:
    `.navbar__links { display: none; }`

    Inside the EXISTING `@media (min-width: 640px)` navbar block (Plan 01 tablet rules), add a rule to hide the hamburger on tablet and up:
    `.navbar__toggle { display: none; }`
    Also add inside that ≥640px block so the drawer never shows on tablet+ even if state were toggled:
    `.navbar__drawer { display: none; }`

    Do NOT touch any non-navbar rule. Do NOT reintroduce `#194d88` or `#fefcfb81`. Use only token var() references plus the one literal `rgba(200, 150, 12, 0.12)` and pixel dimensions (44px, 56px, 4px, 3px) explicitly specified by the UI-SPEC.
  </action>
  <verify>
    <automated>cd /Users/ozt92/Documents/development/sbteknikv2/sbTeknikReact && grep -q "navbar__toggle" src/index.css && grep -q "navbar__drawer-link--active" src/index.css && grep -q "rgba(200, 150, 12, 0.12)" src/index.css && grep -v '^\s*/\*' src/index.css | grep -qc "194d88" ; grep -c "194d88" src/index.css | grep -qx 0 && npm run build 2>&1 | tail -1</automated>
  </verify>
  <done>
    src/index.css contains `.navbar__toggle` (44×44px, accent hover bg), `.navbar__drawer` (surface bg), `.navbar__drawer-link--active` (3px accent left border); `.navbar__links { display: none }` inside `@media (max-width: 640px)`; `.navbar__toggle { display: none }` inside `@media (min-width: 640px)`; zero `#194d88`; `npm run build` exits 0.
  </done>
  <acceptance_criteria>
    - `grep -c "navbar__toggle" src/index.css` returns at least 3 (base + hover/focus + tablet-hide)
    - `grep -c "navbar__drawer-link--active" src/index.css` returns 1
    - `grep -c "width: 44px" src/index.css` returns at least 1
    - `grep -c "rgba(200, 150, 12, 0.12)" src/index.css` returns 1
    - `grep -c "border-left: 3px solid transparent" src/index.css` returns 1
    - `grep -c "var(--color-surface)" src/index.css` includes the drawer rule
    - `grep -c "194d88" src/index.css` returns 0
    - `npm run build` exits 0
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Mobile (<640px) hamburger menu: a 44×44px toggle button (List/X icon swap) that opens an accent-bordered drawer sliding down from below the bar with the three nav links in large Barlow Condensed. Spring animation via motion/react, instant under reduced-motion. Active route shows a 3px gold left border. At ≥640px the hamburger and drawer are hidden and the Plan 01 horizontal links return.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open the localhost URL. Open browser devtools responsive mode at 375px width (mobile).
    2. Confirm horizontal nav links are hidden and a hamburger (List) icon button is visible at top-right.
    3. Tap the hamburger — a drawer should slide down (spring) from below the bar with surface background (`#1a2840`) and three large condensed links; the icon should swap to an X.
    4. Tap a link — the menu should close and the route should change.
    5. Reopen, tap the X — the drawer should close (reverse animation).
    6. Navigate to /services on mobile, reopen the drawer — the Hizmetlerimiz link should show a 3px gold left border + gold text (active).
    7. In OS settings enable "Reduce motion" (or emulate `prefers-reduced-motion` in devtools) — reopen the drawer: it should appear/disappear instantly with no spring.
    8. Resize back to ≥640px — the hamburger disappears and the horizontal links reappear with no layout breakage. Sweep 320px → 1920px to confirm no breakpoint breaks.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any responsive/animation issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none (static read-only) | Static navbar with client-only toggle state. No user input persisted, no API, no auth, no PII. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering (XSS) | Navbar.jsx hamburger/drawer | mitigate | Toggle uses React synthetic `onClick` handlers only (allowed); no `dangerouslySetInnerHTML`, no raw inline DOM-attribute handlers. All drawer copy is static Turkish literals. Acceptance criteria greps `dangerouslySetInnerHTML` returning 0. |
| T-02-04 | Denial of Service | drawer animation | accept | Animation is purely client-side CSS/motion; reduced-motion guard prevents motion-sickness accessibility issues. No resource exhaustion vector on a static page. |
| T-02-SC | Tampering (supply chain) | npm installs | accept | No new packages — useState (react), motion/react, @phosphor-icons/react ListIcon/XIcon all already in package.json. No install task, so no legitimacy gate required. |
</threat_model>

<verification>
- `npm run build` exits 0 with hamburger + drawer added.
- `grep -c "194d88" src/index.css src/components/Navbar.jsx` returns 0.
- `useReducedMotion` guard present and used in the drawer/icon transitions.
- `AnimatePresence` wraps the conditionally rendered drawer.
- Mobile: `.navbar__links { display: none }` under `@media (max-width: 640px)`; hamburger hidden under `@media (min-width: 640px)`.
- Human checkpoint confirms open/close, active drawer state, reduced-motion behavior, and clean breakpoint sweep.
</verification>

<success_criteria>
- NAV-02 (complete): navbar maintains full responsive behavior across all breakpoints — mobile hamburger drawer + tablet/desktop horizontal links, no breakage 320px→1920px.
- NAV-03 (mobile): drawer active state uses accent gold (3px left border + text); hover turns gold. No old corporate-blue highlight.
- Accessibility: hamburger is a `<button>` with toggled `aria-label` + `aria-expanded`; reduced-motion respected.
</success_criteria>

<output>
Create `.planning/phases/02-navbar/02-02-SUMMARY.md` when done.
</output>
